// when i update player destination
// draw move marker at the destination

// when the player has reached the destination
// despawn the move marker

// if the player changes the destination
// despawn the move marker

// draw move marker:
    // despawn old marker
    // spawn new marker

// call spawn and despawn from the player system